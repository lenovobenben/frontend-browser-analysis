interface:
  display_name: "Frontend Browser Analysis"
  short_description: "Analyze frontend runtime state in logged-in Chrome"
  default_prompt: "Use $frontend-browser-analysis to connect to the dedicated logged-in Chrome profile on this Windows or macOS host, inspect the current page, and explain its frontend runtime state and data sources."
